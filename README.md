# NgondroApp

Мобильное Android-приложение для отслеживания буддийских простираний.

## Функции

- 📿 **Счётчик простираний** — ручной или автоматический через камеру
- 📷 **ML Pose Detection** — автоматическое распознавание простираний с помощью ML Kit
- 🔔 **Звук колокольчика** — каждые 100 простираний
- 💾 **Автосохранение** — данные сохраняются при выходе и каждые 100 простираний
- 🗺️ **Карта существ** — (в разработке)
- 📖 **Практики** — (в разработке)

## Технологический стек

- **Flutter** + **Dart**
- **flutter_bloc** — управление состоянием
- **get_it** — внедрение зависимостей
- **go_router** — навигация
- **camera** — работа с камерой
- **google_mlkit_pose_detection** — ML распознавание поз
- **shared_preferences** — локальное хранение данных
- **audioplayers** — воспроизведение звука

## Установка и запуск

### Требования

- Flutter SDK 3.3.0+
- Android SDK (Min API 26 / Android 8.0)
- Android NDK (для ML Kit)

### Шаги

1. Клонируйте репозиторий
2. Перейдите в директорию `ngondro_app`
3. Установите зависимости:
   ```bash
   flutter pub get
   ```
4. Добавьте звуковой файл колокольчика:
   - Поместите файл `bell.mp3` в `assets/sounds/bell.mp3`
5. Запустите приложение:
   ```bash
   flutter run
   ```

## Структура проекта

```
lib/
├── core/          # Константы, тема
├── data/          # Data Layer (DataSource, Repository impl)
├── domain/        # Domain Layer (Entities, Repository interfaces, Use Cases)
├── ml/            # ML Pose Detection
├── presentation/  # UI (Screens, BLoC, Widgets, Navigation)
├── services/      # SoundService
├── di/            # Dependency Injection
├── app.dart       # MaterialApp
└── main.dart      # Entry point
```

## Разрешения Android

- `CAMERA` — для ML-распознавания простираний

## Архитектура

Подробная архитектура описана в [`../plans/architecture.md`](../plans/architecture.md).
